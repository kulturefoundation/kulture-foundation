name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  debug_layout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Show repo layout (safe debug)
        run: |
          echo "PWD: $(pwd)"
          ls -la
          echo "Searching for package.json..."
          find . -maxdepth 6 -name package.json -print -exec dirname {} \;
      
      - name: Stop early (success)
        run: echo "Debug complete — check the Actions log for paths."
```

4. **Commit directly to main** (or create a branch):
   - Scroll down
   - Add commit message: `Debug: Find package.json location`
   - Click **"Commit changes"**

5. **Go to Actions tab** immediately:
   - Click "Actions" at the top
   - Click on the running workflow
   - Wait ~30 seconds for it to complete
   - Click on "debug_layout" job
   - Click on "Show repo layout" step

6. **Look for lines like:**
```
   ./frontend
   ./studio
   ./client
